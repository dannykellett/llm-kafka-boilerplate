"""
Main entry point for the LLM-Kafka Boilerplate application.

This module provides the main entry point for the application when run as a module
(e.g., `python -m src`).
"""

from src.app import main

if __name__ == "__main__":
    main()
